Load project context for this session.

Read and internalize these files:

1. Root CLAUDE.md
2. `apps/frontend/CLAUDE.md`
3. `apps/backend/CLAUDE.md`
4. `apps/proxy/CLAUDE.md`
5. `.claude/sdlc.yml`

Then run:
- `git log --oneline -20` for recent commits
- `git status` for uncommitted work

After loading, provide a brief summary:
- Current branch and recent activity
- Any uncommitted changes
- Key architectural notes from CLAUDE.md files

Keep the summary to 5-10 lines. You are now primed and ready to work.
